<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="this"
             x:Class="Lljxww.ApiCaller.Client.Contents.AuthorizeTypeContent">
    <VerticalStackLayout>

        <CollectionView x:Name="authorizeTypes"
                    ItemsSource="{Binding Auths}"
                    Margin="20"
                    SelectionMode="Multiple"
                    SelectionChanged="authorizeTypes_SelectionChanged">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding Name}" FontSize="22" />
                        <Label Text="{Binding AuthorizationInfo}" FontSize="14" TextColor="Silver"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

        <HorizontalStackLayout>

            <Button
                Text="添加">
            </Button>

            <Button
                Text="删除">
            </Button>

        </HorizontalStackLayout>

    </VerticalStackLayout>
</ContentView>

